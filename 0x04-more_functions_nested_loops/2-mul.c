#include "main.h"

/**
 * _isdigit - Prints the digit
 *
 * @a: input number as an integer.
 * @b: input number as an integer.
 *
 * Return: last digit.
 */

int mul(int a, int b)
{
	return (a * b);
}
